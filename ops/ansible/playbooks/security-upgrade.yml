---
- name: Apply security upgrades
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Refresh apt metadata
      ansible.builtin.apt:
        update_cache: true

    - name: Upgrade packages
      ansible.builtin.apt:
        upgrade: dist
        autoremove: true

